# Benchmark fixture rules

You are solving one scoped coding task in this repository.

- Edit production code under `app/` only.
- Do **not** modify files under `tests/`; the benchmark hashes them before and after your run.
- Run the verification command stated in the task prompt.
- Do not install dependencies or use network access. The suite uses Python's standard library only.
